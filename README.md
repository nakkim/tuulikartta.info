# tuulikartta.info

Display and visualize finnish meteorological observations.

## Docker

````docker
docker build --platform linux/amd64 -t nakkim/tuulikartta-server:v{tag} .
````

````docker
docker push nakkim/tuulikartta-server:v{tag}
````